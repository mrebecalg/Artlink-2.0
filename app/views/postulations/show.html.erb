<p style="color: green"><%= notice %></p>

<%= render @postulation %>

<div>
  <p>
    <%= link_to "Volver", profile_path, class:"btn btn-secondary" %>
    <%= button_to "Cancelar Postulación", @postulation, method: :delete, class:"btn btn-danger" %>
  </p>
</div>
